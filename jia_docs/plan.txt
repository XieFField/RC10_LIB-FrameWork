1. 弄清楚加减速度参数的传递链路（注意修好启动的时候的加速度规划） pass
2. 做 s 型速度规划
3. 做好驱动抑制 pass
4. 了解停车 X 驻车抑制 pass
5. 测试对外接口
6. 做舵轮的断电重复位 pass
7. 做舵轮校准调用接口
8. 舵向断链检测 / 故障锁存 / 恢复后单轮 rehome pass
9. 锁故障即清舵向闭环残留，避免重连首拍吃到 PID 残留输出 pass
10. 修复 fault 锁存阶段 `setTargetCurrent(0)` 被 `RPM/TotalAngle` 覆盖的问题 pass
11. 当前待继续观察：实车若仍存在重连瞬间快速抽动，优先核对 `kSearch` 恢复当拍与电机层 `update()`、CAN 反馈刷新时序
